{
  "name": "mosaic-worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply mosaic-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply mosaic-db --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250317.0",
    "typescript": "^5.4.5",
    "wrangler": "^4.19.0"
  }
}
